# Nuxt app module configuration

This module registers runtime behavior shared by the apps. App-owned Nuxt settings remain in each app's `nuxt.config.ts`, while runtime integrations such as consent-aware analytics live here.
