# Translations

Shared locale messages and date/number formatting for the Nuxt applications. Each app configures its locale files in `nuxt.config.ts`; extendedapp loads an additional message override from `extendedapp/en-US.ts`.

Only English is configured. Shared demo interaction and SEO messages use i18n; the architecture showcase pages currently use English copy directly.
