import { Meta, Typeset } from '@storybook/addon-docs/blocks';

<Meta title="Single App/Typography" />

# Niramit

These are the font weights declared in the shared font stylesheet. The samples use real app copy instead of synthetic thin or black weights.

## Regular · 400

<Typeset fontSizes={[14, 17, 24, 32, 48]} fontWeight={400} sampleText="Shared foundations. Distinct applications." fontFamily="Niramit" />

## Semibold · 600

<Typeset fontSizes={[14, 17, 24, 32, 48]} fontWeight={600} sampleText="Shared foundations. Distinct applications." fontFamily="Niramit" />
