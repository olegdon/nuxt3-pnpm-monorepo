import { Meta, Typeset } from '@storybook/addon-docs/blocks';

<Meta title="Extended App/Typography" />

# Baton

These are the font weights declared in the shared font stylesheet. The samples use real app copy instead of synthetic thin or black weights.

## Regular · 400

<Typeset fontSizes={[14, 17, 24, 32, 48]} fontWeight={400} sampleText="Shared foundations. Distinct applications." fontFamily="Baton" />

## Bold · 700

<Typeset fontSizes={[14, 17, 24, 32, 48]} fontWeight={700} sampleText="Shared foundations. Distinct applications." fontFamily="Baton" />
